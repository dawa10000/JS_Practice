let a = document.getElementsByTagName('div')[0]
// a.innerHTML = a.innerHTML + '<h1>Hello World!</h1>'

let div = document.createElement('div');
div.innerHTML = '<h1>Hello World!</h1>';
// a.appendChild(div);
// a.prepend(div);
// a.before(div);
// a.after(div);
// a.replaceWith(div);
// first.insertAdjacentElement('beforebegin', <div class="test">beforebegin</div>)
// first.insertAdjacentElement('beforeend', <div class="test">beforeend</div>)
// first.insertAdjacentElement('Afterbegin', <div class="test">Afterbegin</div>)
// first.insertAdjacentElement('Afterend', <div class="test">Afterend</div>)
// first.remove()
// first.className = "text-black red"
// first.clasList.remove("red")
// first.clasList.add("red")
// first.clasList.toggle("red")
// first.clasList.contains("red")